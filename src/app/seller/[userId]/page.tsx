import { ComingSoon } from "@/components/site/coming-soon";

export const metadata = { title: "Seller storefront" };

export default function Page() {
  return (
    <ComingSoon
      title="Seller storefront"
      note="Follow sellers, watch their live drops, and shop their full catalogue — coming next."
    />
  );
}
